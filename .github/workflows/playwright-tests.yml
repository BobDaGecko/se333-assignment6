name: Playwright UI Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 1.8
        uses: actions/setup-java@v5
        with:
          java-version: "25"
          distribution: "temurin"
          cache: maven

      - name: Install Playwright dependencies
        run: mvn exec:java -e -D exec.mainClass=com.microsoft.playwright.CLI -D exec.args="install --with-deps"

      - name: Run tests with Maven
        run: mvn clean test

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: target/surefire-reports/
          retention-days: 30

      - name: Upload Playwright videos (Traditional)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-videos-traditional
          path: videos/
          retention-days: 30

      - name: Upload Playwright videos (AI-Generated)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-videos-ai
          path: videos-ai/
          retention-days: 30
